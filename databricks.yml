bundle:
  name: atlikon_project

include:
  - resources/*.yml

targets:
  prod:
    mode: production
    workspace:
      host: https://dbc-1dc6f026-3988.cloud.databricks.com
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}